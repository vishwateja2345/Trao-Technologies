# Copy to .env.local for local development.

# Server-side only (never exposed to the browser): where Next.js proxies
# /api/* requests to. See next.config.ts — this keeps the session cookie
# same-site from the browser's point of view with zero CORS configuration.
API_URL=http://localhost:4000
